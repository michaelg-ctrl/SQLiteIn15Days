To run a query you must navigate in the terminal to the parent folder and then use the following syntax: 

sqlite3 sqlin15days_data.db < [insert query here].sql

For example: 

sqlite3 sqlin15days_data.db < insertmenuitems.sql